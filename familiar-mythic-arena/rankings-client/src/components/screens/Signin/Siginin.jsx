import React from 'react'
import { IMetadata } from 'types'

export const Signin = ({ appName, productName }) => (
  <div>
    <p>Signin</p>
  </div>
)

Signin.propTypes = {
  ...IMetadata
}

export default Signin
